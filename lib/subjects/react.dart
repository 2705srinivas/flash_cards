import 'package:flutter/material.dart';

class React extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('React')),
        body: Container(
          child: Text('react'),
        ));
  }
}
